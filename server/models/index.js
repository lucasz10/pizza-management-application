const Owner = require('./Owner');
const Chef = require('./Chef');
const Topping = require('./Topping');
const Pizza = require('./Pizza');
const User = require('./User')

module.exports = { Owner, Chef, Topping, Pizza, User }